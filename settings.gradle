pluginManagement {
    repositories {
        maven { url "https://maven.crmodders.dev/plugins" }
        maven {
            name "JitPack"
            url "https://jitpack.io"
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

buildscript() {
    repositories {
        maven {
            name "JitPack"
            url "https://jitpack.io"
        }

        mavenCentral()
    }
    dependencies {
        classpath "org.codeberg.CRModders:cosmic-loom:1.0.0"
    }
}

rootProject.name = "PuzzleLoader"
